# 2026-09-21

## 到期提醒（服务通知）不送达 — 定位并修掉根因

- **根因**：`wx.requestSubscribeMessage` 必须由用户 tap 事件同步触发。原实现两处
  （`item-form-sheet.save()`、`quick-entry.persistDrafts()`）都在 `await saveItem()` 之后才
  申请授权 → 每次都被 `fail can only be invoked by user TAP gesture` 拒掉 → 微信侧订阅额度
  恒为 0 → 云端 `subscribeMessage.send` 必然失败 → 服务通知永远不来。
- **修法**：把授权请求提到第一个 `await` 之前（async 函数在首个 await 前的代码仍在 tap
  同步栈里），只把 Promise 留给保存成功后收结果。新增 `requiresReminderSetup()` /
  `hasReminderTarget()` 用同步数据判定「要不要申请」，判据与各自 arm 侧严格对齐。
  提交 `990467d`（分支 `index-ui`）。push 时 VPN 未开、7888 未监听 → **只提交未推送**。
- **微信硬限制**：一次性订阅 = 一次点击换一条额度。批量快速录入 N 条只有 1 条能真正收到，
  其余 `43101`。两条路待龙哥定：维持现状，或加「逐条开启提醒」列表（唯一能绕开限制的做法）。
- **云函数 CLI 跑不了**：`D:\微信web开发者工具\cli.bat` 内部调 `reg.exe`，本机沙箱把 reg.exe
  拉进了程序黑名单，`cloud functions list/download/deploy` 全部被拦且提示不可绕过 →
  云端状态这轮只能用控制台人工确认。
- 新增 `docs/reminder-notification-troubleshooting.md`：云端 4 项待核对清单（MINIPROGRAM_STATE
  与运行版本是否一致、定时触发器、`subscribeMessage.send` 权限、模板字段）、手工造数据触发
  派发的「马上测一条」步骤、`failureCode` 对照表（43101/47003/40037/41030）。

## 体验版能否收到服务通知 — 纠正 `MINIPROGRAM_STATE` 的误述

- 龙哥问「体验版能收到通知吗」。搜证后结论：**能**。服务通知下发到用户微信的
  「服务通知」会话，与小程序跑在哪个版本无关；微信开放社区口径「推送没有体验版这个说法，
  都是推送到用户微信上的」「开发版也是可以触发的」。CSDN 上那些「体验版不支持订阅消息 /
  appid 不同」的帖子是错的，别采信。
- **纠正上一轮自己的说法**：曾称「MINIPROGRAM_STATE 与版本不匹配时消息不展示、接口返回成功」，
  不准确。该参数只定义「跳转小程序类型」（developer/trial/formal，默认 formal），
  只决定**点击通知跳进哪个版本**，不影响能否下发。
  → 已改 `docs/reminder-notification-troubleshooting.md` 第三节表格 + 新增第六节
  （版本相关性只有两项：跳转落点、体验成员名单；必须真机；三条测试路径对比表）。
- 与版本无关的前提：模板已审核通过（不需要小程序上线/认证/备案）、用户有额度、
  云函数有 `subscribeMessage.send` 权限。
- 顺手记下的坑：`scripts/validate-project.mjs:112` 硬断言 `MINIPROGRAM_STATE === 'formal'`，
  所以为体验版改成 `trial` 会直接让 `npm run check` 红。

## 提醒时刻 09:30 → 14:00（为当天下午验证链路）

- 龙哥要求改成下午 2 点，便于当天就测。改了 5 类位置：`domain/reminder-time.ts`、
  `reminderApi/index.js`、`dispatchReminders/config.json`、`cloudbaserc.json`、
  `scripts/validate-project.mjs` 硬断言、`pages/mine/index.wxml` 两处文案，
  以及 4 个测试文件（`reminder-time` / `item-form-sheet` / `item-detail-reminder` 的期望串，
  和 `reminder-time.test.ts` 里写死 9:29/9:30/12:00 的边界时刻 → 13:59/14:00/20:00）。
- **踩到的坑**：`reminder-time.test.ts` 有一条用例用硬编码时刻验 `isReminderMissed` 边界，
  只替换展示串不够，边界值也得跟着挪，否则第一个 `expect` 就挂。
- **关键认知（写进 MEMORY 了）**：`reminderApi.arm` 的 missed 判定是唯一决定「今天还能不能
  落任务」的地方；派发侧 `dispatchReminders` **只比对日期、不看时钟**，
  所以不等定时器、在控制台手动「云端测试 → 运行」就能立刻派发。
- 文档 `docs/reminder-notification-troubleshooting.md` 同步：开头加变更声明，第三节触发器
  那行强调「触发器不随代码更新」，第四节重写为「路径 A 走真实链路 / 路径 B 手工造数据」。
- `npm run check` 全绿（36 文件 / 469 测试）。
