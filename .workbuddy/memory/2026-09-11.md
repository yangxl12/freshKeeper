# 2026-09-11

## 完整录入表单改版（item-form-sheet）
- 表单顺序调整为：物品名称 → 到期计算 → 到期日期/生产日期+保质期+预计到期 → 提前提醒(+开关) → 数量(+单位小输入框) → 分类 → 存放位置。
- 单位行删除，融入数量行末尾：固定 128rpx 小输入框 `.quantity-unit-input`，默认「件」，仍是独立 `unit` 字段（校验 1~8 字符不变）。
- 提前提醒行末尾加 `<switch>`（`reminderEnabled`，默认开）：无论开/关都弹出「提醒设置」弹窗（复刻 mine 页，组件样式隔离需自带 modal 样式），保存走 `updateSettings({defaultReminderLeadDays})` 并同步回表单 `reminderLeadDays`。关闭开关仅禁用输入框（`number-suffix-control--disabled`），保存 payload 不变（后端无"关闭"概念，提醒开关真正的语义在 item-detail）。
- `npm run check` 通过；commit e9063f6 已推 index-ui。
