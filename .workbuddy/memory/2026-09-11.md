# 2026-09-11

## 完整录入表单：提前提醒去开关 + 新增提醒授权开关（commit 051908f，已推 index-ui）

改动只在 `miniprogram/components/item-form-sheet/`（ts / wxml / wxss），
影响「物品录入-完整录入」、快速录入的完整录入 tab、编辑、重新入库四处。

- 「提前提醒」行去掉行尾 switch，输入框不再有 `disabled` / `number-suffix-control--disabled`；
  删掉 `handleReminderSwitch` 与 `reminderEnabled` 数据字段。
- 新增「提醒授权」行（在提前提醒下方），switch 的 checked 绑 `subscriptionAuthorized === true`。
- 提醒设置弹窗补上「提醒授权」行 + 「查看设置」按钮，跟「我的—提醒设置」同款。

### 关键平台约束（别再踩）
- **微信订阅消息授权是系统级状态，小程序既写不进也关不掉**。唯一数据源是
  `wx.getSetting({ withSubscriptions: true })` → `subscriptionsSetting.mainSwitch`
  （`undefined` = 用户从未授权过，微信不下发状态，当未开启处理）。
  改授权只能 `wx.openSetting({ withSubscriptions: true })` 让用户自己点。
- 因此「提醒授权」开关**故意不落任何本地状态**：`bindchange` 里不读 `event.detail.value`，
  只弹弹窗 + 引导去系统页。若真写成受控开关，用户拨到关会立刻回弹 → 假开关。
- **开关回弹技巧**：拨动后 switch 内部状态已变，setData 同值不一定刷回。用
  `authSwitchRebuilding` + `wx:if` 把 switch 卸载重建（在弹窗遮罩盖住表单后做，用户无感）。
- 同步靠"每次重新读"而不是"存下来"：组件 `lifetimes.attached` + `pageLifetimes.show`
  各读一次，`closeReminderSettings` 和 `openSetting` 的 complete 再兜底读。
  `pageLifetimes` 在本项目首次使用，`this` 类型正常（strict 模式下 tsc 通过）。

### 备注
- `npm run check` 全绿：204 tests / 15 files。
- 组件 `index.json` 只有 `{"component": true}`，即 isolated 隔离，app.wxss 的 class 样式
  进不来；`pressable--pressed` 这类 hover 在组件内是否真生效存疑（既存问题，未扩大范围）。

## 修 bug：提醒授权关了仍显示「通知已开启」（commit a17957e，已推 index-ui）

龙哥反馈：在微信设置里关掉授权后，弹窗里「提醒授权」还是「通知已开启」。

**根因（探针实测确认，不是缓存）**：`wx.getSetting({withSubscriptions:true})` 真实返回是
`{ mainSwitch: true, itemSettings: { <模板ID>: 'reject' } }` —— 用户关掉的是**「临期提醒」这个模板**的授权，
`mainSwitch`（订阅消息总开关）根本没变。第一版实现只读 `mainSwitch`，判据用错了。

- 修法：`services/reminder-service.ts` 新增纯函数 `resolveReminderAuthorization` +
  异步 `readReminderAuthorization`，折算四态 `authorized / main-switch-off / template-rejected / unrequested`；
  「完整录入」和「我的—提醒设置」两处消费端改为共用它，删掉 mine 页原来的 `updateSubscriptionSummary`。
- 补 `tests/unit/reminder-authorization.test.ts` 7 个用例（reject / ban / 无记录 / 总开关关 / 未下发）。
- `npm run check` 全绿：211 tests / 16 files。

### 探针实测手法（复用）
`cli.bat auto` 起端口 9420 → 复制 `~/AppData/Local/Temp/wx-probe/run-state.mjs` 改 probe 路径 →
node 跑 runner。**查小程序 API 返回值不需要云函数**，`miniProgram.evaluate` 里包个 Promise 调 `wx.xxx` 就行。
实测对比：同一条真实数据下，旧判据得 `true`（错），新判据得 `false`（对）。

## 快录页四项调整：上限 20 / 倒序 / 卡片进编辑复用完整录入表单 / 光标对齐

文件：`pages/quick-entry/{ts,wxml,wxss}`、`components/item-form-sheet/{ts,wxml}`、
`domain/quick-entry.ts`、两个测试文件、`docs/quick-entry-acceptance.md`。

- **1 条上限 20**：`MAX_DRAFTS` 5→20（只改客户端累积上限；云端「一次解析最多 5 条」不动，
  那是 `date-facts.js` / `quick-text.js` 的单请求上限）。新增派生数据 `draftLimitReached`
  （`countUnfinished(drafts) >= MAX_DRAFTS`，已入库卡片不占名额），
  「从最近录入添加」`disabled="{{saving || recognitionState !== 'idle' || draftLimitReached}}"`，
  并在输入卡片下方给一行 `feature-hint` 说明（沿用「置灰必须给说明」的约定）。
- **2 编辑页复用完整录入表单**：给 `item-form-sheet` 加 `purpose` 属性（`'save'` 默认 / `'draft'`）。
  draft 模式下 `save()` 不写库，改为 `triggerEvent('draftsubmit', collectDraftFields())`；
  快录页把旧的 `.draft-editor` 内嵌表单整块删掉，换成整屏 overlay + `<item-form-sheet id="draftForm" purpose="draft">`，
  底部沿用组件自带的 fixed `.save-bar` 当「完成」按钮（所以别给 overlay 加底部按钮）。
  **组件新加 `dirty` 跟踪 + `isDirty()`**：快录页退出时问一次（改过才问），没改动直接退。
  domain 新增 `draftToFormPrefill()`（如实回填，含冲突日期；别用 `draftToManualFields`，
  它会把有问题的字段裁掉）和 `applyFormValuesToDraft()`（**日期没动过就保留 dateConflict**，
  否则「没改就点完成」会静默吃掉日期冲突）。
  同时删掉了旧的 `handleTextInput/handleDateChange/handleModeChange/chooseCandidate/confirmFallback`
  和整块 `.qe-*` / `.candidate-*` / 页级 `.mode-switch` 样式——编辑表单只有一份了。
  **代价**：草稿编辑里不再有日期候选列表，且「到期计算」切模式会像完整录入一样清空另一种模式的值。
- **3 后添加的在前 + 末条被遮挡**：`generateDrafts` 改成 `[...built.drafts, ...existing]`（新一批整批插顶、批内保持原文顺序），
  `selectRecent` / `recognizePhoto` 也改成前插；`editingIndex` 随之从 `drafts.length` 变 `0`。
  遮挡是因为 `.quick-footer` 是 fixed，`.quick-body` 只留了 24rpx —— 加 `.quick-body--with-footer`（204rpx + 安全区）。
- **4 输入框光标与 placeholder 错位**：根因是 `.quick-input` 上 `line-height: 76rpx`：
  Android 微信原生 textarea 的**文字/光标按 line-height 居中、placeholder 行内居顶**，差距随 line-height 线性放大。
  修法：行高改回 36rpx（贴合 28rpx 字号）、76rpx 高度由 `padding: 20rpx 6rpx` 撑出，
  placeholder 单独给 `placeholder-class="quick-input-placeholder"`（同 font-size/line-height），
  textarea 补 `disable-default-padding="{{true}}"` 去掉 iOS 默认内边距。
  **这条是平台行为，别再给 textarea 设大 line-height。**

### 测试
`npm run check` 全绿：220 tests / 16 files。改顺序/改编辑入口后重写了这些用例：
「卡片任意位置进编辑且只在完成时回写」「带改动退出二次确认 / 没改动直接退」
「复用 item-form-sheet 作编辑页」「上限 20 与置灰」「后添加在前 + 底部让位」，
并把 `handleTextInput`/`handleDateChange` 描述的用例改成走表单提交或直接测 domain 纯函数。

## 修正上一条：草稿编辑改回「底部弹窗」，只复用表单本体（同 df9e6cb 之后）

龙哥反馈：编辑不该整屏复用完整录入页，要原来那种弹窗，**只复用里面的表单**。

- `pages/quick-entry/index.wxml` 还原弹窗外壳：`.draft-editor`（fixed 容器，`justify-content:flex-end`）
  \+ `.draft-editor__mask`（`catchtouchmove="noop"` 挡穿透）+ `.draft-editor__panel`（82vh 圆角面板，
  `entry-sheet-up` 动画）+ `__head`（居中标题 + 手柄，相机/关闭绝对定位）+ `__body` + `__foot`（取消 / 完成）。
  wxss 里 `@keyframes entry-sheet-up` 必须一并还原（上一版把 keyframes 删了，弹窗会没有升起动画）。
- **组件不再渲染自己的 save-bar**：不是加 `hideSaveBar` 属性，而是直接判 `purpose !== 'draft'`。
  原因：自定义组件属性在 WXML 里要写 kebab-case（`hide-save-bar`），这条映射没在项目里验证过，
  一旦不生效就会在弹窗里叠出两排按钮；`purpose="draft"` 本身就意味着「宿主持有按钮」，语义也更准。
  同理根节点加 `.form-sheet-page--inline`（去掉 `padding-bottom: 154rpx` 的 save-bar 占位）。
- 快录页新增 `confirmDraftEditor()`（把 `save()` 转给表单）和 `noop()`；`requestCloseDraftEditor` 不变。
- **顺手修的竞态**：组件 `applyPrefill` 注入草稿后，`attached` 时发出的 `loadDefaults()` 若迟到，
  会用「设置里的默认提前提醒天数」覆盖草稿值。加 `data.prefilled` 标记，`loadDefaults` 里
  `if (!pendingDraft && (this.data.itemId || this.data.prefilled)) return`；`resetEntry` 复位该标记。
- `docs/quick-entry-acceptance.md` 的「统一草稿」行改为描述底部弹窗。

### 测试与工程教训
- `npm run check` 全绿：221 tests / 16 files。
- 用例改名 `keeps the bottom-sheet chrome and reuses only the full-entry form inside it`（改成整段切片断言
  mask/panel/cancel/confirm + 组件里 `purpose !== 'draft'` 的 save-bar），新增
  `submits the shared form when the sheet 完成 is tapped`；`stubDraftForm` 补 `save()` 替身。
- **本机 bash 没有 coreutils**（`sed`/`head`/`wc` 全 `command not found`），git 输出转存一律走 PowerShell。
- **PowerShell 的 stdout 会被吞**（`Write-Output`/`Select-String` 都不回显），命令结果要 `Out-File` 落盘再 Read。
- **一次消息里对同一文件发多个 Edit 会互相覆盖丢改动**：本次 `.form-sheet-page` 根 class 的修改被同批的
  另一处 Edit 覆盖掉了，复查时才发现。同一文件的多处改动要顺序发。

## 「从最近录入添加」改成独立页面（多选批量带回）

龙哥反馈伪页面体验差：只能点右上角 × 退出、点一条要叠底部弹窗、连选多条要来回跳。

- 新增 `pages/recent-entry/index.{ts,wxml,wxss,json}`，`app.json` 注册（排在 quick-entry 之后）。
  原生导航栏返回 + 名称搜索 + 多选，底部固定条「添加到录入（N）」。
- **通信走 eventChannel**，不引全局状态：
  quick-entry `wx.navigateTo({ url: '/pages/recent-entry/index?slots=N', events: { pickedDrafts: (p) => this.appendRecentDrafts(p.drafts) } })`；
  新页面 `getOpenerEventChannel().emit('pickedDrafts', { drafts })` 后 `wx.navigateBack()`。
  `emit` 是同步的，回到录入页时草稿已落进列表；不带草稿返回则录入页状态不变。
- `slots = MAX_DRAFTS - countUnfinished(drafts)`，新页面 onLoad 里再 `Math.min(20, ...)` 夹一次。
  回传的草稿由 `appendRecentDrafts()` 整批插最前并按剩余名额二次截断。
- **独立 `editorOpen` 开关**：弹窗显示不能判 `editingIndex >= 0` —— 新选的草稿还没进 `picked`，
  下标就是 `-1`，第一版因此弹窗根本不渲染。已选项再点开是修改（`editingPicked=true`），
  弹窗底部左侧按钮随之变成「移出已选」。
- quick-entry 侧删干净：`quickTab` / `popup` / `editingRecentNew` / `loading` / `loadingError` /
  `selectRecent` / `closeRecentList` / `retryRecent`；`loadRecentProfiles` → `refreshRecentProfiles`（静默）。
  **`recentProfiles` 保留**：`buildDraftsFromText` 还要用它给识别结果匹配分类和存放位置。
- WXSS：recent-* 样式整块从 quick-entry 搬进新页面；`@keyframes` 是**页面级作用域**，
  新页面必须自己定义 `recent-mask-in` / `recent-sheet-up` / `recent-footer-up`，引 quick-entry 的会没动画。
- 测试：删/迁 4 个伪页面用例，新增 `tests/unit/recent-entry-page.test.ts`（17 个用例）。
  `npm run check` 全绿：17 files。
- 坑：测试里给 mock 用 `mockResolvedValueOnce` 时，`onLoad` 自己会拉一次列表，
  Once 会被 onLoad 吃掉，后续调用拿到 undefined → 静默变空列表。桩要能应答多次。

## 修 bug：草稿编辑里切换「到期日期 ↔ 保质期计算」清空已填数据（commit f8acc42，已推 index-ui）

龙哥反馈：快录页确认卡片进编辑，弹窗里从「到期日期」切到「保质期计算」再切回来，到期日没了。

- **根因**：`components/item-form-sheet/index.ts` 的 `handleModeChange` 每次切换都 setData 把**对侧**字段置空
  ——切到 `shelf_life` 清 `expiryDate`，切回 `direct` 清 `productionDate` / `shelfLifeValue`。
  这是本文件上方 4c8f15a 那次「只复用表单本体」一起带进来的行为（当时在日志里记成了"代价"）。
- **修法**：只 `setData({ mode, dirty: true }, () => this.updateExpiryPreview())`，不清任何字段。
  **根本不需要清**：`save()` / `collectDraftFields()` 都按当前 mode 取字段、未选中侧固定写 null；
  云端 `inventoryApi/validation.js` 同样按 mode 归一化。三处一致，不存在串味。
- 顺带收益：切回 `shelf_life` 时 preview 会用保留的 productionDate + 保质期立刻重算（原逻辑把 preview 也清了）。
- 新增 `tests/unit/item-form-sheet.test.ts`（4 例）：来回切不丢到期日 / 来回切不丢生产日期+保质期且 preview 恢复 /
  回传字段仍按 mode 取一侧 / 重复点当前 mode 不置 dirty。
  组件测试的可复用套路：stub 全局 `Component` 收 definition → 实例 = `{...methods, data: 初始 data + properties 初值}`，
  `setData` 用顶层 `Object.assign`（本项目假 setData 不认路径 key），依赖模块全部 `vi.mock`。
- `npm run check` 全绿：241 tests / 18 files。
- **遗留（未修，属额外范围）**：切模式仍置 `dirty: true`，用户只是切去看一眼又切回来，退出也会被问一次"有改动"。
  严格说 `expiryInputMode` 确实变了（回传值不同），要做准得记录切换前快照。

## 代码审查：未提交的「数量变化动效」（inventory-row + home/index.ts）

范围：工作区未提交改动 + f8acc42。`npm run check` 全绿（241 tests / 18 files，EXIT=0）。
改动内容：`inventory-row` 新增 `quantityFlash` / `lastQuantity` + `'item.quantity'` observer +
`flashQuantity()`（先摘 class 再 nextTick 加回，保证连续点按能重放），wxml 把 class 挂到数字上，
wxss 加 up/down 两组 keyframes + `prefers-reduced-motion` 降级；`home/index.ts` 的 `applyQuantity`
**删掉「数量已改为 X」toast**，改由动效承担反馈。

审查结论（逻辑正确，无需改功能）：
- `wx:key="_id"` 保证组件不会跨 item 复用，`lastQuantity` 初值 -1 挡掉首次触发 → 方向判定不会误闪。
- patchItem 重建对象引用 → `'item.quantity'` 必然触发；失败路径不 patch 所以不闪，符合注释。

待处理（未修）：
1. 未跟踪残留 `probe-env.txt` / `probe-fixed.txt`（内容就是一份 git status 快照）。
   ⚠️ 当时写的是「项目没有 .gitignore」——**这条是错的**，根因是 `cat` 在 bash 里不可用，
   命令静默失败走了 `||` 分支。`.gitignore` 一直存在，缺的只是 `probe-*` 规则。
2. 动效零测试，和上一 commit「修 bug 必补组件测试」的标准不一致；方向判定适合抽纯函数测。
3. 删 toast 的代价：读屏用户失去数量变化的语音反馈（该页 wxml 的 aria 写得很足，这里退化）。
4. `.item-card__quantity` 是 `overflow: hidden`，4 位数量放大 1.16 倍有被裁风险，要真机确认。
5. `flashQuantity` 的 `wx.nextTick` 回调没做卸载保护，460ms 内组件销毁会 setData 警告。

环境坑（新）：本机 bash 跑不了 npm —— `npm run check` 在 Bash 工具里报
`/usr/bin/env: 'bash': No such file or directory`（PATH 里的 shim 脚本自己也在报 `dirname: command not found`）。
**npm 脚本一律用 PowerShell 工具跑**，输出 `Out-File` 落盘再 Read（`npm.cmd` 走 Volta 正常）。

## 四件事落地（commit 226f3ab，已推 index-ui）

龙哥按上一轮审查结论逐条点单，一次做完：

1. **删残留 + 补忽略**：删 `probe-env.txt` / `probe-fixed.txt`；
   `.gitignore` 加 `probe-*.txt`（连同注释）。**没有覆盖已有规则**——先 Read 才发现它一直存在。
2. **补动效测试**：新增 `tests/unit/inventory-row.test.ts` 11 例，覆盖首次不播 / up / down /
   同值不播 / 非整数忽略 + 基准不被污染 / 连续同向变化每帧都重放（这条锁住「先摘 class 再挂回」，
   否则同 tone 第二次不会重启动画）。组件测试套路同 `item-form-sheet.test.ts`：
   stub `Component` 收 definition + stub `wx.nextTick` 入队手动 flush + `wx.vibrateShort` 用 spy。
3. **补轻提示**：`flashQuantity` 里加 `wx.vibrateShort({ type: 'light', fail: () => {} })`
   （失败静默，不影响主流程）+ 隐藏 live 文本 `quantityA11yText`（`aria-live="polite"`）。
   **`aria-live` 在微信端能否真的播报未实测**——写了无害，但别当成已生效的能力对外承诺。
4. **修输入框太窄**：根因是 `.item-card` 的 grid 第三列固定 194rpx，里面塞两个 58rpx 的 ± 按钮后，
   中间只剩 ~74rpx；`.item-card__quantity-input` 又是 `flex: 1 1 0`，实际宽度约 48rpx，
   4 位数只剩一条缝（外层 `overflow: hidden` 还会直接裁掉）。
   修法：**编辑态让 ± 按钮收起让位**（wxml `wx:if="{{!editing}}"` + `item-card__stepper--editing`
   改 `justify-content: center`），输入框改 `flex: 0 0 auto`，宽度由 `quantityInputWidth()`
   按字数分档 56/76/96/116rpx 走 inline style 下发；提交后复位。展示态一行没动。

### 本机环境坑（这次踩得最深，已写进 MEMORY.md）
- **bash 里的 `rm` 也被包装过且是坏的**：`safe-bin/rm` 报 `dirname: command not found` → `safe_delete_main: command not found`，exit 127。
- **沙箱内 `Remove-Item` 静默失效**：exit 0、`Test-Path` 还是 True。要加 `dangerouslyDisableSandbox`。
- **真正的删除是平台 safe-delete（走回收站）**：沙箱外执行时抛
  `[safe-delete][SAFE_DELETE_FAIL_CLOSED] ... genie-trash failed; refusing fallback delete`，
  但文件**实际已进回收站**（`Test-Path` False）。所以"报错"和"没删掉"不能划等号，**必须 `Test-Path` 复核**。
- **`npm run check` 在 bash 里跑不了**：`/usr/bin/env: 'bash': No such file or directory`。
  PowerShell 里先 `$env:Path = "C:\Program Files\Volta;" + $env:Path` 再 `npm`，正常。
- Write 整个文件前必须 Read，且 **Read 记录跨轮次会失效**（报 `File has not been read yet`），要重新 Read。
- `npm run check` 全绿：**252 tests / 19 files**（新增 11）。

## 用户体系现状分析（只读，未动代码）

龙哥问「项目用户这块怎么设计的」。结论：**没有用户体系，只有用户身份 = 微信 OPENID**。

- 身份来源唯一：`cloud.getWXContext().OPENID`。客户端**不做 `wx.login` / `getUserProfile`**，
  `app.ts` 只 `wx.cloud.init({ traceUser: true })`（traceUser 只进云开发控制台的访问记录，不在业务数据里）。
  **无 users 集合、无 unionid、无手机号、无自建登录**。
- 数据归属：3 个集合全靠 `ownerId` 字段（= OPENID）隔离。`inventory_items` 全部查询/索引以 ownerId 打头；
  `user_settings` 用 `_id = ownerId` 做单例（一人一档，懒创建）；`reminder_jobs` `_id = itemId`（一物品一任务）。
- 防伪造三件套：① `assertNoClientIdentity` / `assertSafeEvent` 拒绝客户端传 `ownerId/openid/openId/_openid/templateId`（各函数各有一份，**未抽公共模块**）；
  ② 每次读写都带 `ownerId` 条件，**禁止只按 `_id` 直接更新**；③ 集合客户端权限设「无权限」，只允许云函数访问（见 `docs/cloud-deployment.md` 第 2 节）。
- 云存储也按用户软隔离：快录媒体 `quick-entry/<sha256(openid)[:32]>/<type>/...`（`quickEntryApi/validation.js:mediaOwnerPrefix` + `validateMedia` 比对前缀），
  封面图 `sha1(ownerId)[:16]`（`inventoryApi/image-cover.js:coverCloudPath`）。都是**哈希后入路径**，明文的 OPENID 不落存储路径。
- **用户资料是假的**：`pages/mine/index.ts` 的昵称/头像走 `wx.setStorageSync('mine_profile')` + `saveFileSync` 到 `USER_DATA_PATH`，
  默认昵称「保质记用户」。换设备/清缓存即丢，云端完全不存 → 多端不一致。**头像/昵称未入库、无 user profile 集合。**
- 文档明确列入「不做」：独立账号 / 手机号登录 / 用户资料系统（`mvp-technical-design.md:35`）；
  家庭成员邀请 / 多人共享 / 权限管理（`mvp-product-design.md:52`）。产品定位写的是「**单个微信用户**管理自己的家庭库存」。
- **缺口（若要上架/做用户体系）**：
  ① 无数据导出与「账号注销/清空我的数据」入口 —— 微信审核 + 个保法通常要求，是硬性缺口；
  ② 无用户维度生命周期记录（首次使用、最后活跃），要留存/活跃只能挖控制台 traceUser 或日志；
  ③ 无 unionid → 未来接公众号/多小程序/App 无法合并同一微信用户的数据；
  ④ OPENID 绑环境，换 AppID/云环境数据即孤儿，无迁移出口。
- 扩展路径（按代价递增）：A 加 `users` 集合（`_id = OPENID`，首次调用 upsert）——纯档案、不动现有集合；
  B 把 mine 的 profile 从本地迁到 `users`；C 家庭共享需引 `households` + `members`，`ownerId` → `householdId`，**涉及全量数据迁移 + 双写**，代价最高。

## 写方案：`docs/user-account-plan.md`（A 档，未动代码）

A 档拆成 A1（users 集合 + userApi + 启动 touch，纯增量）/ A2（账号注销，合规刚需）两阶段。
关键决策都写进文档了，捡几条别忘的：

- **不在 4 个业务函数入口 upsert users**（写放大 + 活跃度被调用次数污染），新增独立 `userApi`，
  客户端 `onLaunch` 调一次 `touch`。**不要放 `onShow`**（前后台切换会反复触发）。
- `touch` 双层节流：客户端本地存 `user_touch_date` 比对上海日期 + 服务端判 `lastSeenAt` 是否已是今天。
- 注销**顺序不能错：先分批读出 items 收集 `coverFileId` → `cloud.deleteFile`（单次上限 50）→ 再删 DB**
  （单次 remove 上限 1000，外层循环 + 最多 20 轮）。反过来就收集不到 fileID。
- 注销**不做墓碑文档**，直接删干净（留 `deletedAt` 反而是残留个人信息）；重入即新用户，天然幂等。
- **`userApi/config.json` 必须首建前就写 `timeout: 60`**（本项目已知坑：首建后 deploy 不重新应用）。
- 昵称/头像上云归 B 档，A 档只预留 `nickname` / `avatarFileId` 字段（值 null）。
- 注销 UX 要两步 `wx.showModal`，结果提示**不能用 toast**（7 汉字截断），用 modal 或页面内状态。
- `users` 也写冗余 `ownerId`，跟 `user_settings` 的 `_id: ownerId, ownerId` 双写保持一致。

## 写方案：`docs/user-profile-plan.md`（B 档，未动代码）

B1 = 昵称头像上云（多端一致），B2 = 数据导出。关键点：

- **客户端上传云存储的既有通道**：`quick-entry-service.ts:uploadQuickEntryMedia` 是
  「云函数发 cloudPath → 客户端 `wx.cloud.uploadFile({cloudPath, filePath})` → 拿 fileID」。
  头像复用这套，**别自己发明写法**（客户端能直接上传是既成事实）。
- 头像路径 `avatars/<sha256(openid)[:32]>/<ts>-<uuid>.<ext>`，与 `covers/`、`quick-entry/` 同族；
  服务端 `updateProfile` 必须**前缀比对 fileID 归属**（照抄 `quickEntryApi/validation.js:validateMedia`），
  否则用户能把别人的 fileID 挂自己头上。
- **微信 `<image>` 直接吃 `cloud://` fileID**，`mine/index.wxml` 一行不用改。
- `chooseAvatar` 给的是**临时路径**，不是 fileID，必须先上传；上传前 `wx.compressImage(quality:80)`，
  压缩失败退回原图继续（别中断）。
- 换头像后**异步删旧图、失败静默**（对齐 `image-cover.js` 的 fire-and-forget），孤儿可接受。
- **代理对陷阱**：现有 `profileNickname.trim().slice(0,20)` 会把 emoji 昵称切成方块；
  input 的 `maxlength` 也按 UTF-16 计数。客户端+服务端都改 `Array.from(x).slice(0,20).join('')`。
- 存量迁移必须带**"非默认值"条件**（本地昵称 !== '保质记用户' 或头像非空），
  否则把占位昵称灌进 `users` 纯噪音。迁移放"打开资料弹窗"，**不放 onLaunch**；
  本地头像文件可能已失效，失败只迁昵称。
- **导出用 `.txt` 装 JSON**：`wx.openDocument` 不支持 json 也不支持 txt；
  `shareFileMessage` 对 `.json` 支持不稳 → 只有 `.txt` 走得通。走云存储中转是因为
  云函数响应体约 1MB 上限，物品多了直接返回必然超。
- 导出计数**落库不落实例内存**（复用 ai-quota 的实例内存思路在这里是错的，冷启动会清空）。
- 待真机验证两点：① `type="nickname"` + 键盘自动填充时 `bindinput` 是否触发（部分 Android 不触发）；
  ② `wx.shareFileMessage` 对 `.txt` 的实际行为（项目里唯一没验证过的 API）。
- A 档文档已加交叉引用指向本文件。

## A 档落地：users 集合 + userApi + 启动 touch + 账号注销（已实现，待部署）

按 `docs/user-account-plan.md` 一次性做完 A1 + A2，`npm run check` 全绿：288 tests / 22 files。

新增云函数 `cloudfunctions/userApi/`（`index.js` / `error.js` / `validation.js` / `date.js` /
`account.js` + config+package+lock，config 已写 `timeout: 60`）：
- **`account.js` 是核心，注入式 `createAccountService({ db, deleteFile })`** —— 和
  `image-cover.js` 同款思路，index.js 才 require `wx-server-sdk`，单测注入假 db 不加载真 SDK。
  纯逻辑（`shouldTouchToday` / `chunk`）分别落在 `date.js` 和 `account.js` 里可独立测。
- action：`touch` / `get` / `updateProfile`（B 档才用，先实现完就位）/ `deleteAccount`。
- 注销顺序：收集 coverFileId → `deleteFile`（50/批）→ remove 四个集合（`users` 按 `_id`）。
  `removeAll` 循环 + 20 轮上限，超限抛 `DELETE_INCOMPLETE`（可重试，天然幂等）。
- 业务错误码**不在 message 里**，测试必须用 `error.code` 断言（`toThrow('CODE')` 会失败）。
- `package-lock.json` 直接从 settingsApi 复制后改 name（103 个包，别手写）。

客户端：
- `miniprogram/services/user-service.ts`：`touchUser` / `getUserProfile` / `deleteAccount` /
  **`touchUserOnceToday()`**（同日节流塞进 service，app.ts 一行调用，测试不用 stub `App()`）。
- `utils/shanghai-time.ts` 新增 `shanghaiTodayKey()`（复用 `date-key.ts:formatDateKey`）。
- `app.ts` onLaunch 在 `wx.cloud.init` 后调 `touchUserOnceToday()`，失败静默。
- mine 页第 6 个 entry `data-entry="account"`「账号与数据」→ 弹窗 → `startDeleteAccount()`
  两步 modal（第二步 `confirmText:'确认注销'` / `confirmColor:'#A33F32'`）→ `showLoading`
  → `deleteAccount()` → `clearStorageSync()` → 结果 modal → `reLaunch` 首页。
  `closeModal` 加 `deletingAccount` 保护；失败不 reLaunch、不 clearStorage、复位标志位。
  图标 `.entry__icon--account`（红 #b84a3e），按钮复用 app.wxss 里既有的 `.danger-button`。
- `docs/cloud-deployment.md` 补：users 集合、userApi 部署与权限、3.0 节部署注意事项、验收两条。

**还没做的（要人工）**：控制台建 `users` 集合（权限「无权限」）→ 首建部署 userApi（确认 timeout 60）
→ 前端发版；「用户隐私保护指引」补注销入口说明（审核项，代码代替不了）。

## 云端部署 + 模拟器真跑验证（已完成）

- **我用 CLI 直接部署了 userApi**：`D:\微信web开发者工具\cli.bat cloud functions deploy
  --env cloud1-d0gkh66ce94b1be08 --names userApi --project D:/myProject/freshKeeper --remote-npm-install`。
  首建报 `FailedOperation.UpdateFunctionCode 当前函数处于 Creating 状态` → **等 45 秒重跑同一条命令**即可
  （第二次走 "exists in the cloud, will update it"）。
- **新发现：CLI 部署不应用 `config.json` 的 timeout**，首建完查 info 还是 3 秒（runtime Nodejs16.13，
  与其他函数一致）。已在云开发控制台改成 60（`cli cloud functions info` 复核通过）。
  → **`docs/cloud-deployment.md` 里"首建会读 config.json"的说法是错的，每次新建函数都要手动改超时。**
- **模拟器真调验证通过**（automator `miniProgram.evaluate` 里包 Promise 调 `wx.cloud.callFunction`）：
  `touch` → `{created:false, lastSeenAt:'2026-09-11'}`（首次建档发生在 App.onLaunch，
  probe 的两次都命中服务端同日节流）；`get` → `{nickname:null, createdAt:'2026-09-11',
  lastSeenAt:'2026-09-11'}`（证明 `users` 里真有这条记录）；未知 action → `INVALID_ACTION`。
  探针：`C:/Users/BYS/AppData/Local/Temp/wx-probe/probe-user-api.js` + `run-user-api.mjs`。

## B 档落地：昵称头像上云 + 数据导出（commit 17771f6，已推 index-ui）

按 `docs/user-profile-plan.md` 做完 B1 + B2，`npm run check` 全绿：**348 tests / 25 files**。

云端 `userApi`：
- `validation.js` 新增 `avatarOwnerHash` / `avatarCloudPath(hash, ext, now, id)` / `normalizeNickname`
  / `exportCloudPath` / `exportFileName`（时间戳用 `Intl` 按上海时区取，文件名
  `保质记-数据导出-YYYYMMDD-HHmm.txt`）。**昵称改成按码点截断**（`Array.from`），超长不再拒。
- 新增 `export.js`：`buildExportPayload` 纯函数，剔除 `_id/ownerId/_openid/templateId`，
  reminder 只留 `remindAt/status/createdAt/inventoryItemId`。
- `account.js`：`createAvatarUpload`（只回 cloudPath，路径里是 openid 哈希）、
  `updateProfile` **换头像成功后 fire-and-forget 删旧图（失败只记日志、不回滚）**、
  `exportData`（`readAll` 分批 → buildExportPayload → `uploadFile` 的 `fileContent: Buffer`
  → 写 `lastExportedAt` / `exportCountDate` / `exportCount`，每日 3 次 `EXPORT_LIMIT_EXCEEDED`）、
  `deleteAccount` 额外收集 `users.avatarFileId`。service 入参多了注入式 `uploadFile`。

客户端：
- `utils/nickname.ts`（`normalizeNickname` / `displayNickname`）、
  `utils/profile-migration.ts`（`shouldMigrateProfile`）。
- `user-service.ts` 新增 `updateProfile` / `createAvatarUpload` / `uploadAvatarFile`
  （compressImage quality 80 → 取 cloudPath → `wx.cloud.uploadFile`，压缩失败退回原图）/
  `exportData` / `shareExportedFile`（download → `wx.shareFileMessage` → 清云端+本地副本，
  用户取消返回 false 不算错）。
- mine 页：`onShow` 里 `Promise.all([loadSettings(), loadProfile()])`；`loadProfile` 60s 节流
  （模块级 `profileReadAt`），**云端失败静默降级用本地缓存，不进错误态**；`saveProfile` 走
  `updateProfile` 且防重入；`chooseAvatar` 即选即传即写档案（不跟保存按钮）；存量迁移挂在
  打开资料弹窗路径上；「账号与数据」弹窗加「导出我的数据」按钮。
- 本地 `mine_profile` 降级为**缓存 + 离线兜底**，`profile_migrated` 标记防重复迁移。

### 这次踩的坑（复用价值高）
- 假 db 的 `get()` **必须返回副本**，否则 `update` 会就地改掉之前读出来的对象引用，
  导致「换头像删旧图」判断成"新旧相同"而漏删（真 db 不会，但测试会假阳性/假阴性）。
- 测试里 stub `wx.shareFileMessage` 这类返回 Promise 的 API 时**必须 return 那个 promise**，
  否则 `await undefined` 直接成功，取消分支测不到，还会冒 unhandled rejection。
- 服务端抛的**原生 Error 没有 `code`**（`AppError` 才有），断言错误码要用 AppError 路径，
  原生异常改成 `rejects.toThrow`。
- 迁移 payload **不能带 `avatarFileId: null`**：本地头像已是 `cloud://` 时会把云端头像清空。
  改成只在真的上传成功时才写这个字段（`shouldMigrateProfile` 也随之要求"头像还是本地文件"）。
- 页面测试里断言"保存失败后留在弹窗"前要先 `setData({profileVisible:true})`，默认实例是 false。

### 部署与发版状态（2026-09-11 14:50）
- 云端 `userApi` 现状：`cloud functions info` → `status Active / timeout 60 / Nodejs16.13`。
  **timeout 还是 A 档时我在控制台手改的 60**，更新部署不会重置它，所以这次不用再开控制台。
- 更新部署命令（实测跑到 "will update it" 后失败）：
  `& 'D:\微信web开发者工具\cli.bat' cloud functions deploy --env cloud1-d0gkh66ce94b1be08 --names userApi --project D:/myProject/freshKeeper --remote-npm-install`
- **失败原因：本机代理 127.0.0.1:7888 没监听** → `ECONNRESET / tunneling socket could not be
  established, cause=connect ECONNREFUSED 127.0.0.1:7888`。
  排查过：`HKCU\...\Internet Settings` 的 **ProxyEnable=0**（系统代理是关的），但 ProxyServer 值
  残留 `127.0.0.1:7888`；在子进程里清 `HTTP(S)_PROXY/ALL_PROXY` 环境变量**无效** →
  来源不是 env，是开发者工具自己的代理设置（设置→代理）或代理软件没开。
  解法：① 打开监听 7888 的代理软件；② 开发者工具里把代理改成「不使用代理」。然后重跑同一条命令。
  **reg.exe 在本机被安全策略黑名单拦截，别用它查注册表**，用 PowerShell 的 `Get-ItemProperty`。
- 前端发版只能人工（开发者工具「上传」+ mp 后台提交审核），且**必须先更新《用户隐私保护指引》**：
  `open-type="chooseAvatar"` 与 `type="nickname"` 属隐私接口，指引没声明头像/昵称会被平台直接拦。

### 云端测试面板不能用来验收云函数（2026-09-11 15:00 实测）
龙哥在云开发控制台「云端测试」里调 `userApi`，日志是
`{"action":"","resultCode":"UNAUTHENTICATED","durationMs":0}`。
**这是必然结果，不是 bug**：控制台调用不带微信上下文，`cloud.getWXContext().OPENID` 为空，
而 `userApi/index.js` 的 OPENID 断言在 `handlers[action]` 之前，所以**任何 action 都只会回
UNAUTHENTICATED**，跟代码是 A 档还是 B 档无关。这条日志只能证明：函数在线、Init 正常
（require 链完整，缺 `export.js` 会在 InitFunction 阶段就崩）。
**验收需要 OPENID 的云函数只有两条路**：模拟器真调（automator `wx.cloud.callFunction`）或真机，
控制台测试一律作废。判断"代码到底传上去没有"最快的办法是看控制台函数文件列表里有没有
`export.js`（B 档新增文件）+ `account.js` 里搜 `createAvatarUpload`。

### 还没做的（要人工 / 真机）
- 重新部署 `userApi`（更新部署，timeout 沿用 60）+ 前端发版。
- 《用户隐私保护指引》补「收集昵称与头像用于个人资料展示」。
- 真机验证：`type="nickname"` 自动填充时 `bindinput` 是否触发（不触发要补 `bindblur`）；
  `wx.shareFileMessage` 对 `.txt` 的实际表现（导出链路唯一没验证过的 API）。

