# 2026-09-12

## 到期提醒去开关化（commit 983f924，已推送 index-ui）

把「单品开关 + 详情页提醒弹窗」整套模型推翻，改成订阅消息自动推送。

- 删：表单「入库后开启到期提醒」勾选、详情页提醒弹窗与「提醒/取消提醒」按钮、
  quick-entry 草稿的 `remindAfterSave`、`reminderApi` 的 `cancel` action + `rules.canArmReminder/canCancelReminder`。
- 加：`miniprogram/domain/reminder-time.ts`（提醒时间 = 到期日 − 提前天数，当天 09:30；
  `resolveReminderTime` / `isReminderMissed` / `formatReminderText`），表单与详情都只读展示。
- 授权接入点只剩「保存物品时申请一次」；编辑时仅缺额度（无任务/failed/cancelled）才重新申请。
- 派发触发器 09:00 → **09:30**；提醒只认当天，`remindDate < today` 一律 cancelled + `REMINDER_MISSED`；
  `arm` 遇已过返回 `{status:'missed'}` 且**不落任务**。
- 「我的 → 提醒设置」保留默认提前天数，新增微信通知授权状态行 + 去微信设置。

### 教训 / 坑
- **`Number(null) === 0`**：提前天数做校验必须先排 null/undefined 再 Number()，否则
  「没填」会被当成「到期当天提醒」。单测锁死了这条。
- **前端拦截只看日期（`reminder.date < today`），不看时钟**：前端拿真实时钟判断会让行为
  随运行时刻漂移，测试也跟着变成时间炸弹。当天 09:30 是否已过交给云端 arm 再判。
- **展示用的 `missed` 与拦截用的日期比较是两套口径**，已在类型注释里写明。
- 前端单测里跟「未来时刻」相关的用例一律用 2099 年日期，别用近两年日期。

### 未完成（阻塞在外部）
模板 ID 仍是占位串 `TODO_REPLACE_WITH_REAL_TEMPLATE_ID`。需要在公众平台建模板
「物品保质期到期提醒」（物品名称/到期日期/物品类型/存放位置/数量）后替换三处：
`miniprogram/config/runtime.ts`、`cloudfunctions/reminderApi/index.js`、
`cloudfunctions/dispatchReminders/template.js`。并且必须去云开发控制台改触发器时间
（`config.json` 的 `triggers`/`envVariables` 只在首次创建时写入云端，改了不生效），
顺手删掉控制台里残留的 5 个 `REMINDER_*_FIELD` 与 `REMINDER_TEMPLATE_ID` 环境变量。

### 环境
- `Remove-Item` 在这个环境里静默失效（沙箱内外都删不掉），
  **`git clean -fx -- <明确路径>` 是唯一可靠的删文件手段**（.log 被 gitignore 时要带 -x）。
- `npm run check` 全绿：415 tests / 29 files。

## 性能分析报告（docs/performance-analysis.md）

全量走读了 miniprogram（381.7 KB / 34 ts）+ 7 个云函数，产出 `docs/performance-analysis.md`。
**只写文档，未改任何代码。**

定位到的关键结论（供后续优化用）：

- **`listRecentProfiles` 单次最多 24 次 DB 查询**（`inventoryApi/recent.js:65-88`）：
  `limit=100` + `MAX_RECENT_ROUNDS=12`，数据不足 100 条时 `cutoff` 逻辑退化成跑满 12 轮 ×2 状态；
  每轮还对累积 rows 全量重排。**小数据量用户反而最亏**。修法：加提前退出 / 建 `ownerId+updatedAt` 索引改单次查询。
- **首页 onShow 固定 5 次 DB 读**（4 次 `count` + 1 次列表），无缓存、无脏标记，
  详情页返回也重来。云开发 `count()` 是扫计数，比列表查询贵。
- **分页用 skip(offset)**（`index.js:183/306/587/614`），offset 硬上限 10000 且成本随页码线性。
  改成 `(expiryDate, createdAt)` 复合游标**不需要新索引**（现有索引已覆盖）。
- **批量操作页 O(N²) 下发**（`batch-operation/index.ts:95-125`）：do/while 拉全量 + 每页全量 setData，
  N=1000 时约 16 倍冗余。最省事的修法是「先累积、最后一次 setData」。
- **`dispatchReminders` 逐条串行**（`index.js:238-253`）：每条 6-8 次 DB + 1 次 openapi，
  50 条/批 × 20 批 ≈ 250s，超 60s 超时 → **超时后剩下的提醒第二天会被判 REMINDER_MISSED 直接取消，用户永远收不到**。
  改 8 路并发即落到 ~32s。**这项优先级应最高**（会丢用户数据语义）。
- 其他：搜索正则三字段 OR 里 `{name: keyword}` 是纯冗余（`searchName` 已是小写 name）；
  写操作事务外 `getOwnedItem` 与事务内读重复；`processBatch` 20 并发事务 / `cleanupTrash` 100 并发；
  `generateCover` 的 `name` 等值查询没有索引；封面 1024² 原图直出且无 `lazy-load`。

报告里明确列了「不建议做的事」：不分包（体积太小）、不上虚拟列表、不去掉 version 并发控制、
不调大 `cloud.ai()` 超时预算。

## 排查「推上去了却一直显示 69 个待推送」

**结论：远端已是最新，69 是假象。**
- `git ls-remote origin` → `refs/heads/index-ui` = `d8c6499` = 本地 HEAD，一条都没差。
- 假象来源：`.git/packed-refs` 里残留一条 **9/9 的老记录** `417b21e refs/remotes/origin/index-ui`，
  而真正的松散引用目录 `.git/refs/remotes/origin/` **不存在**（9/10 那次 `.git/refs` 被清空后只 `mkdir -p` 到 `remotes` 层，没补 `origin`）。
  git 拿 packed-refs 那条老值去比 → 永远 ahead 69。

**关键发现：带斜杠的引用在本环境里建不出来（待龙哥自己终端复核）**
- `git branch probe/nested` → **静默失败**：分支不生成、`refs/heads/probe/` 目录不生成、零报错。
- `git update-ref refs/heads/a/b <sha>`、`refs/remotes/origin/main` 同样静默失败。
- 同环境里 `refs/remotes/zztest2`、`refs/heads/index-ui`（都是**单层文件**）能写成功。
- **两套 git 都复现**：`C:\Program Files\Git` 的 2.53.0.windows.2 + WorkBuddy 自带 PortableGit 2.55.0.windows.3；
  连全新 `git init` 的空仓库也复现 → 大概率是我这边执行环境（沙箱/进程级拦截）所致，**不一定是龙哥本机 git 坏了**。
- 且 `git fetch`/`git update-ref` 一跑，会把我手工补出来的 `refs/remotes/origin/` 目录连带删掉，所以手工补目录只是临时状态。

**已做的处理（只在 `.git` 内，未动工作区）**
- 备份 `.git/packed-refs` → `.git/packed-refs.bak-20260912`。
- 清掉 packed-refs 里那条过期记录（现只剩表头）→ status 从假的 `[ahead 69]` 变成诚实的 `[gone]`，不再谎报待推送。
- 清掉全部诊断临时文件，撤销测试用空提交 `aaf3936`，HEAD 回到 `d8c6499`，`--porcelain` 为空。

**待验证**：龙哥在自己终端跑 `git fetch origin` + `git status`。恢复成干净的
`## index-ui...origin/index-ui` 就说明只是我这边沙箱问题；仍是 `[gone]` 就是本机 git 建不了
`refs/remotes/origin/`，需重装/换版本 Git 或查公司安全软件。

**排查手法（可复用）**：先 `git ls-remote origin` 拿远端权威值，再和 `git rev-parse HEAD` 对比——
只要两者相等，「待推送 N」就是本地引用坏了，跟提交本身无关。

## 落地性能优化第一批（commit 6e09437，**未推送：GCM 401**）

按 `docs/performance-analysis.md` 第 1 批清单改完，`npm run check` 全绿（421 tests / 30 files）。

### 云端（`cloudfunctions/`）
- `inventoryApi/recent.js`：`readRecentProfiles` 加提前退出（唯一名字数 ≥ `limit × RECENT_STOP_FACTOR(2)` 即 break），
  `cutoff` 逻辑保留但不再退化成跑满 12 轮。**最坏 24 次查询 → 6 次以内**。
  同时去掉「每轮对累积 rows 全量重排」（O(rounds × n log n)）——改成只对新增 slice 排序。
- `inventoryApi/index.js`：
  - `listInventory` 删掉 `{ name: keyword }` 分支（`searchName` 写入即 `name.toLowerCase`，纯冗余）。
  - **删掉 `listActive` 死代码**（函数本体 + `handlers` 注册）。
  - `save`/`transition`/`moveToTrash`/`removePermanently`/`restore` **删掉事务外的 `getOwnedItem`**
    （事务内 `getTransactionOwnedDoc` + `assert` 已覆盖存在性）→ 每次写省 1 个 RTT。
  - `processBatch` 加 `BATCH_CONCURRENCY = 5` 受控分批（原 20 路并发事务）。
- `dispatchReminders/index.js`：内层派发改 `JOB_CONCURRENCY = 8` 受控并发（250s → ~32s，落进 60s 超时），
  单条 catch 兜住不掀翻整批；**新增 `remaining` 观测字段**（原来超时截断时运维看不出漏了多少）。

### 前端（`miniprogram/`）
- `components/inventory-row`：`<image lazy-load="{{true}}">`；封面改用 `coverThumb`。
  **注意 observer 的封面身份判据仍是原图 `coverFileId`**（`coverFor`），`coverThumb` 只是它派生的展示 URL，
  否则就绪回填/换图逻辑会乱。
- `domain/inventory.ts`：新增 `coverThumbUrl()`（拼 `?imageView2/2/w/200/h/200/format/webp/q/80`）、
  `MAX_LIST_ITEMS = 200` + `canLoadMoreItems()`、`InventoryCardItem.coverThumb`。
- `pages/batch-operation/index.ts`：`loadAll` 改成**先累积、按 `EMIT_BATCH_SIZE(100)` 档位下发**，
  9 页 setData 从 9 次 → 4 次。**这个改动让原来那个「分页拉完再允许全选」的测试挂了**——
  因为 `listTrashMock.mockResolvedValue` 是一次性值，循环第 3 页后拿不到新页。
  测试里改用 `mockImplementation(() => Promise.resolve(pages.shift() ?? trashPage([])))`；且 9 页 × 30 = **270** 不是 250。
- `pages/home/index.ts`：**概览 SWR 缓存**（`home_overview_cache` + `overviewDirty` 脏标记 + `applyCachedOverview`），
  `refreshOverview(options?: {force?})` 支持穿透；写操作调 `invalidateOverview()`。
  **跨日失效不依赖 `scheduleMidnightRefresh` 的 setTimeout**（小程序切后台 5 分钟 JS 挂起，定时器基本不会准时触发），
  而是每次 onShow 比 `cache.dateKey !== shanghaiTodayKey()`。
  列表加 200 上限 → `loadMoreLimited` 态 + 提示文案。
- `pages/mine/index.ts`：`loadSettings(force = false)` 加 `PROFILE_READ_TTL_MS` 节流（原来只有 `loadProfile` 有）；
  `readReminderAuthorization()` 与云端读**并行**（原串行）；保存后重置 `settingsReadAt`。回收站列表同样加 200 上限。
- `components/item-form-sheet/index.ts`：新增 `derivedPatch()`（纯计算）+ `computeExpiryPreview()`；
  `refreshDerived()` 改成一次 setData；`updateExpiryPreview()` 加「值没变就 return」短路；
  `handleTextInput` **先写 `this.data[field]` 再 `setData({...})`** 以复用 `derivedPatch()`（原来每字符 3 次 setData）。

### 文档
- `docs/cloud-deployment.md` 索引表补 `inventory_items: ownerId ASC, inventoryStatus ASC, name ASC`（支持 `generateCover` 同名查询）。
- `docs/performance-analysis.md` 新增「0. 实施进度」表，逐项标注 ✅ / ⏳。

### 坑 / 教训
- **`Get-Content` 读中文测试输出会乱码**（GBK/UTF-8 混淆），断言失败信息要靠 `Select-String`
  匹配 ASCII 关键字（`Test Files` / `expected`）来定位，别指望肉眼读中文用例名。
- PowerShell 里 `npm test 2>&1 | Out-String | Set-Content` 的 stdout 正常，但直接管道给
  `Select-Object -Last N` 常返回空 → **统一先落文件再 Read**。
- `git push` 失败但**退出码捕获不到错误文本**（`$LASTEXITCODE` 只给 128，`2>&1 | Out-String` 是空的）。
  可靠做法：`git push origin <branch> 2> .err.txt` 把 stderr 重定向到文件 + `$env:GIT_CURL_VERBOSE=1`。
- **本次 push 的真实原因：HTTP 401 + `www-authenticate: Basic realm="GitHub"`**，
  代理隧道本身正常（CONNECT 200 / TLS1.3 握手成功 / `x-github-edge-region: japaneast`）。
  `credential.helper` 全局是 GCM，但**拿不出有效凭据**。这不是网络问题，是认证问题 ——
  需要龙哥那边触发一次 GCM 浏览器 OAuth 授权，或者改用 SSH/换 PAT。**没往远端写任何东西**。

## 落地性能优化第三/四批（14-17，**按龙哥要求只改不提交**）

`npm run check` 全绿：**446 tests / 33 files**（原 421 / 30）。

### 14 批量操作去事务化 → 新增 `cloudfunctions/inventoryApi/writes.js`
- `transition` / `moveToTrash` / `removePermanently` / `restore` 从 `db.runTransaction`
  改成「读一次 + `where({_id, ownerId, inventoryStatus, version})` 条件更新」，
  `updatedCount/removedCount !== 1` → `CONFLICT`。`version` 进 where 就是乐观锁。
- **读没省**：报告 4.3 建议「全并成 CONFLICT」，但那会让批量结果里「已删除」被说成「刷新重试」。
  保留读才能区分 `NOT_FOUND` / `INVALID_STATE` / `CONFLICT`。
- **取消提醒挪出事务是安全的**：`dispatchReminders.processJob` 发送前会重新校验物品状态，
  漏掉的任务会被判 `ITEM_NOT_ELIGIBLE` 取消，**不会误发**（这是敢去事务的前提，已写进注释）。
- `save` / `saveIdempotent` 保留事务：要保护「物品 + 提醒改期」和幂等键。
- `writes.js` 用 `createWriteService({ db })` 注入式（对齐 `userApi/account.js` 范式），
  新增 `tests/unit/inventory-writes.test.ts`（12 用例）覆盖错误码不退化、乐观锁、越权。

### 15 settingsApi 并入 userApi
- 新增 `cloudfunctions/userApi/settings.js`（`createSettingsService({db})`），
  `userApi/index.js` 注册 `getSettings` / `updateSettings`；`cloudfunctions/settingsApi/` **已删**。
- 前端 `services/settings-service.ts` 改调 `userApi`。**前后端必须一起发布**。
- 顺手删掉 `getSettings` 里的 `hasReminderJobs`：那是一次 `reminder_jobs` 的 count，
  全项目没有任何地方渲染它，等于每次读设置白扫一张表。
- `updateSettings` 改成「条件更新优先，没命中再 `set`」，省掉更新前那次存在性读。

### 16 快录页派生值入草稿对象
- 11 个平行数组（`statusTones[]` / `expiryBadges[]` …）合并成 `draft.view`
  （`types/quick-entry.ts:QuickEntryDraftView`），`commitDrafts` 从 14 个 key 降到 4 个，
  顺带修掉「平行数组与 drafts 索引错位」的隐患。
- 原本还在算的 `draftSummaries` **没有任何渲染方**，一并删。
- 测试里新增 `draftBody()` / `draftBodies()` 剥掉 `view` 再比草稿本体。

### 17 前端耗时埋点
- `utils/analytics.ts` 新增 `trackDuration(event, startedAt, data)` 统一补 `durationMs`。
- `home_first_screen`（onLoad 起算，失败不清零）/ `home_list_refresh` / `home_list_more` /
  `item_save_result`（收口在 `saveItem`，`updateQuantity` 传 `reason:'quantity'` 免得污染分布）。
- **微信自定义分析要在后台登记事件名**，没登记的会被平台静默丢弃（不报错）。

### 17 后续：埋点改双写（同一轮，接龙哥决策）
- 查文档发现 **`wx.reportAnalytics` 已停止维护**（官方：基础库 2.31.1 起改用 `wx.reportEvent`）。
  两者是**两套互不相通的后台系统**（mp「统计→自定义分析」vs We分析「数据管理→上报管理→元事件」）。
- 龙哥选「双写过渡」+ 确认 25 个事件**从没在后台登记过**（一直空转）。
  `utils/analytics.ts` 新增私有 `report()`：先 `wx.reportEvent`（`typeof` 守卫，低版本基础库跳过）
  再 `wx.reportAnalytics`，各自 try/catch。数据稳定后删后者分支即可。
- 新增 **`docs/analytics-events.md`**：14 个属性清单 + 25 个事件清单 + 两套后台登记路径 + 测试验收步骤
  （We分析事件详情页「测试」→ 选微信号 → 手机走一遍 → 1 分钟后看最近 1000 条，错误会标红）。
- **后续龙哥拍板砍掉老的**：只留 `wx.reportEvent`，删掉双写分支。
- **漏网事件**：`app.ts:31` 直接调 `wx.reportAnalytics('app_open')`，没走封装，所以不在清单里 ——
  已改成 `track('app_open')`，清单补上（25 → 26 个）。**教训：埋点必须全走 `analytics.ts` 封装**。
- `npm run check`：**447 tests / 33 files**。单测加了「两边都发」和「无 reportEvent 时回退老接口」两个场景。

### 17 后续②：批量 JSON（接龙哥截图，属性「批量JSON创建」弹窗）
- 拿到后台真实 schema：`key`(小写+数字+下划线,64)/`desc`(中文名)/`comment`/`key_type`
  (KEY_TYPE_STRING|数值被截断，按 KEY_TYPE_INT 填)/`dict_name`。**属性 ID 不允许驼峰**。
- **代码上报字段全改 snake_case**：`durationMs→duration_ms`、`failureCode→failure_code`、
  `savedCount→saved_count`、`draftCount→draft_count`、`candidateCount→candidate_count`、
  `within24h→within_24h`（`count/result/reason/source/tab/succeeded/failed` 本就合规）。
  改动点：`analytics.ts`(trackDuration)、`quick-entry/index.ts` 6 处、`item-form-sheet` 1 处、
  `tests/unit/analytics.test.ts` 断言。
- 新增 `docs/analytics-properties-batch.json`（纯数组，直接粘贴批量创建）；
  `analytics-events.json`/`analytics-events.md` 同步改。
- 校验：14 属性全匹配 `^[a-z][a-z0-9_]*$`、批量文件与清单 key 一致、事件引用无坏 ID。
- **事件那边的批量 schema 已拿到**（龙哥截图）：`event_id`(小写开头+数字下划线,64,保存后不可改)/
  `event_name`(中文,64,不可重复)/`event_comment`(200,可选)/`report_src`(number,默认0=前端)/
  `event_key_list`(array<string>,只能填已建属性 id)。
  已出 `docs/analytics-events-batch.json`（26 个，report_src 省略）。
  校验过：ID 合规、event_key_list 全部命中属性批量文件里的 key、无重复。
- `npm run check` 全绿 447 / 33。

### 新坑
- **`git clean -fx` 只对未跟踪文件生效**：删已跟踪目录（`cloudfunctions/settingsApi`）它不动，
  必须用 `git rm -r -f <dir>`。之前记的「删文件只认 git clean」是错的，已改。
- `git rm -r` 可能留下 `.git/index.lock`，之后所有 git 命令都报
  `fatal: Unable to create '.git/index.lock': File exists`。**删掉这个锁文件即可恢复**（确认没有别的 git 在跑）。
- **本机没有 pwsh 7**（`C:\Users\BYS\AppData\Local\Programs\PowerShell\7\pwsh.exe` 不存在，
  记忆里那条路径已失效）。直接用 PowerShell 工具跑 `npm run check`，输出 `Out-File` 后 Read。
- **重复 Read 同一个临时文件名会拿到上一轮的旧内容**（命令实际没跑成功时尤其明显）。
  每轮换个文件名（`.c1` `.c2` …）才靠谱。

