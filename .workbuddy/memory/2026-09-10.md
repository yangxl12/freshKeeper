# 2026-09-10

## 首页「更多」弹窗：图标重做 + 层级修复

- 图标：新增 `miniprogram/assets/icons/action-{edit,done,delete,remind}.svg`（32×32，圆角底 + 24 栅格线稿，
  `transform="translate(4 4) scale(0.75)"`，stroke 2.6、round cap/join）。语义配色沿用状态色：
  编辑=绿 #1f5d49 / #eef4f1，已用完=蓝 #3d7f9c / #eaf4f9（与「已用完」统计卡一致），删除=红 #b84a3e，提醒=琥珀 #a96524。
  wxml 由 CSS 伪元素拼图标改为 `<image src="/assets/icons/xxx.svg" mode="aspectFit">`（48rpx）。
- 层级：自定义 tabBar（`custom: true`）可能与页面遮罩不在同一层叠上下文，遮罩 z-index 提到 4000 **不够保险**。
  改为自定义 tabBar 增加 `hidden` 态（`.tabbar--hidden { opacity:0; translateY(120%); pointer-events:none }`，180ms 过渡），
  首页 `openMore/openQuantity` 调 `setTabBarHidden(true)`，`closeMore/closeQuantity` 与 `onShow`(syncTabBar) 复位 false。
  注意 `.tabbar__add` 是 `position: fixed`，祖先有 transform 后会以 `.tabbar` 为包含块——因为两者底边/宽度一致，视觉不变。

## Git 对象丢失的恢复（重要）

- 现象：`git status` 突然报 `fatal: bad object HEAD`，`git fsck` 显示大量 dangling/无效 refs，松散对象只剩 7 个
  （历史提交对象全丢）。怀疑与 `git stash push -- <path>` 失败（报 `d1b17d82... is not a valid object`）同时发生。
- 恢复：`git ls-remote origin` 发现远端 `refs/heads/index-ui` 与本地 HEAD 同点（历史都已推送），
  直接 `git fetch origin` 即把缺失对象拉回，随后 `git log/status/commit/push` 全部正常。
- 遗留：`refs/codex/turn-diffs/checkpoints/...` 三条 ref 指向不存在的对象，会让 `git fetch` 报
  `did not send all necessary objects`（但不影响 commit/push）。需要时可删掉这几条 codex 检查点 ref。
- 教训：本仓库历史一定要及时 push，远端是唯一的兜底备份；不要在仓库里跑失败风险高的 stash。

## 其他

- `npm run check` 中 `tests/unit/quick-entry-page.test.ts` 的 4 个遗留失败已修好（见下），现全量 110 测试通过。

## 快速录入：提交后收起输入法 + 确认卡片重做

- 焦点问题根因：`.quick-input` textarea 开了 `hold-keyboard="{{true}}"`，点「确定」（form submit）不会自动收起键盘。
  新增 `blurQuickInput()` = `wx.hideKeyboard()` + `quickInputFocused:false` + `quickKeyboardHeight:0`，
  在 `generateDrafts` 开始时、以及生成成功后各调一次（`openRecentList`/`openFullTab` 也统一改用它）。
- 顺带去掉 `focusNameId` 自动聚焦名称输入框——它会立刻把键盘再弹起来，与需求冲突；
  改为 `nameMissingFlags`（`issues.some(i => i.field === 'name')`）驱动 `.name-field__input--missing` 高亮。
- 确认卡片重做（wxml/wxss）：`draft-card` 加 `overflow:hidden` + 右上 `__glow` 径向渐变光斑 + 300ms `draft-card-in` 入场；
  头部 `序号徽标 + 来源 + 状态 pill + 删除`；名称单独放大成 `.name-field`（32rpx/650）；
  新增 `.expiry-hero` 到期信息主视觉（大号 DIN 日期 + 「还剩 N 天/已过期 N 天/待补充」徽章，fresh/soon/expired/empty 四色）；
  待补问题改成 chip（`.draft-card__issue`）；底部摘要改成 `.draft-card__summary-text` + `.draft-card__more`（不再是 `> text:first-child`）。
- 派生数据统一在 `draftView(drafts)` 里算：`expiryBadges / expiryTones / statusLabels / statusTones / sourceLabels / nameMissingFlags`，
  `commitDrafts` 走它，`handleTextInput` 单条更新时也要同步这几条数组，否则视图会脱节。
- 顺手修的 4 个遗留失败：`startVoice`/`chooseDatePhoto` 前置 `capabilities.*` 守卫（未接入时静默返回）；
  `saveDrafts`/`selectableCount` 尊重 `draft.selected`（`countSelectable()`）。
- 视觉预览（不参与编译）放 `docs/preview/quick-entry-card.html`，按 750rpx=375px 换算，可直接浏览器打开评审。
